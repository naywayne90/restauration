import PlaceholderPage from "@/components/placeholder-page";
import { Package } from "lucide-react";

export default function StockPage() {
  return <PlaceholderPage title="Stock" icon={Package} />;
}
