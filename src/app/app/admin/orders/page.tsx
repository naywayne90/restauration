import PlaceholderPage from "@/components/placeholder-page";
import { ShoppingCart } from "lucide-react";

export default function OrdersPage() {
  return <PlaceholderPage title="Commandes" icon={ShoppingCart} />;
}
