import PlaceholderPage from "@/components/placeholder-page";
import { Settings } from "lucide-react";

export default function SettingsPage() {
  return <PlaceholderPage title="Configuration" icon={Settings} />;
}
