import PlaceholderPage from "@/components/placeholder-page";
import { BookOpen } from "lucide-react";

export default function EmployeeMenuPage() {
  return <PlaceholderPage title="Menu du jour" icon={BookOpen} />;
}
